<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zerobase.fastlms.loginHistory.mapper.LoginHistoryMapper">


    <select id="selectList" resultType="com.zerobase.fastlms.loginHistory.dto.LoginHistoryDto">
        select lh.*
        from login_history lh
        where login_id = #{userId}
    </select>

</mapper>
